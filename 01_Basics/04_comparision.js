// here we won't get any issue, rather if we compare 2 diff DT's that would make problem to solve. 
 
console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);

// comparision converts null to a number, treating it as 0. 